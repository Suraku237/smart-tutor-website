name: Deploy Landing Page
on:
  push:
    branches: [ main ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: 🚚 Copy HTML to VPS
        uses: appleboy/scp-action@master
        with:
          host: "109.199.120.38"
          username: "root"
          key: ${{ secrets.VPS_SSH_KEY }}
          source: "*" # This sends your index.html and assets
          target: "/var/www/SMART-TUTOR-WEBSITE" # Adjust to your web root
      - name: ✅ Deployment Complete
        run: echo "Landing page deployed successfully!"